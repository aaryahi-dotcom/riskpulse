# Copy to .env.local for local overrides (Vite reads .env files from this
# directory, not the repo root .env — that one is for docker-compose /
# the backend). Falls back to http://localhost:8000 if unset.
VITE_API_BASE_URL=http://localhost:8000
