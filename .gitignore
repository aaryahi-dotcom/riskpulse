# --- Logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- Frontend / Node ---
node_modules/
dist/
dist-ssr/
*.local

# --- Python ---
**/__pycache__/
*.py[cod]
**/.venv/
.venv/
venv/
*.egg-info/

# --- ML artifacts / trained models ---
# Committed (not regenerated) because retraining needs the 683MB Kaggle
# IEEE-CIS dataset below, which isn't in the repo and isn't practical to
# fetch in a Render build — so the live model artifacts ship as-is.
# checklist 2.6: archived previous model generations (rollback source),
# written by POST /api/v1/admin/retrain before it overwrites the live
# artifacts — these stay gitignored, same as before.
backend/models/archive/

# --- Databases ---
*.db
*.sqlite3
backend/*.db
backend/*.sqlite3

# --- Raw dataset (never committed, 683MB+26MB) ---
data/

# --- Env / secrets ---
.env
.env.local
*.env.local

# --- Editor / OS ---
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
